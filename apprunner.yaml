version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install @cyanheads/pubmed-mcp-server supergateway
run:
  command: ./node_modules/.bin/supergateway --port 8080 --stdio "node ./node_modules/@cyanheads/pubmed-mcp-server/dist/index.js"
  network:
    port: 8080
